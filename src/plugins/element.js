import Vue from 'vue'
import {
  Button,
  Input,
  Container,
  Main,
  Aside,
  Collapse,
  CollapseItem,
  Header,
  Popover,
  Row,
  Col
} from 'element-ui'


Vue.use(Button).use(Input).use(Container).use(Main).use(Row)
   .use(Aside).use(Collapse).use(CollapseItem).use(Header)
   .use(Popover).use(Col)